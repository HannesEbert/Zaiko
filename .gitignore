# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ / Android Studio
*.iml
*.ipr
*.iws
.idea/

# VS Code — personal state is ignored, shared config is committed
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
!.vscode/extensions.json

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/

# Note: pubspec.lock IS committed — this is an application, not a library,
# so builds must be reproducible with exact dependency versions.

# Test coverage
coverage/

# FVM — SDK versions are cached locally, only .fvmrc is committed
.fvm/

# Personal Obsidian vault — kept outside the repo, never committed
Vault/

# dotenv environment variables
.env*

# Generated JavaScript (web builds)
*.dart.js
*.info.json
*.js_
*.js.deps
*.js.map
